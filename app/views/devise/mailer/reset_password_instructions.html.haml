%p= "#{t('.hello')} #{@resource.email}!"
%p= "#{t('.someone_has_requested_a_link_to_change_your_password')} #{t('.you_can_do_this_through_the_link_below')}"
%p= link_to t('.change_my_password'), edit_password_url(@resource, :reset_password_token => @token)
%p= t '.please_ignore_this_email'
%p= t '.your_password_wont_change_until'
