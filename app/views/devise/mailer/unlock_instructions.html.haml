%p= "#{t('.hello')} #{@resource.email}!"
%p= t '.your_account_has_been_locked'
%p= t '.click_to_unlock_your_account'
%p= link_to t('.unlock_my_account'), unlock_url(@resource, :unlock_token => @token)
